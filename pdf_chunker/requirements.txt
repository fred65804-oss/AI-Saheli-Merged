# PDF chunker — standalone, self-contained (no project coupling). Free & local.

pypdfium2>=4            # render PDF pages to images (permissive license)
rapidocr-onnxruntime   # free, pip-only OCR engine (default backend)
onnxruntime>=1.17      # RapidOCR runtime
numpy
Pillow>=10
wordninja              # repairs OCR space-drops ("coordinateand" -> "coordinate and")

# Windows only: ships the MSVC runtime DLLs that onnxruntime needs, so no
# system Visual C++ install is required.
msvc-runtime; platform_system == "Windows"

# ---- Optional: higher-accuracy OCR + Hindi (recommended for production) ----
# One-time free install of the Tesseract binary, then use  --ocr tesseract
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki  (or choco install tesseract)
#   pip install pytesseract
#   Hindi: install the `hin` traineddata and edit TesseractBackend(lang="eng+hin")

# ---- Optional: exact token counts ----
# tiktoken>=0.7        # then pass --tokenizer tiktoken
